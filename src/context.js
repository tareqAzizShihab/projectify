import { createContext } from "react";
const FormModelContext = createContext();
const TasksReducerContext = createContext();
const SearchContext = createContext();

export { FormModelContext, SearchContext, TasksReducerContext };
